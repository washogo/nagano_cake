ja:
  enums:
    order:
      payment_method:
        credit_card: 'クレジットカード'
        transfer: '銀行振込'

#ja: # 日本語の設定を行う
  #enums: # enumの設定を行う
    #[モデル名]:
      #[カラム名]:
        #[enumで番号と紐付けた要素]:
        #[enumで番号と紐付けた要素]: # 要素の数だけ追加する
